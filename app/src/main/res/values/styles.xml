<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- for normal text -->
    <style name="MyTextTheme">
        <item name="android:textColor">@android:color/black</item>
    </style>
    <style name="MyTextTheme.Small" parent="MyTextTheme">
        <item name="android:textSize">10sp</item>
    </style>
    <style name="MyTextTheme.Medium" parent="MyTextTheme">
        <item name="android:textSize">13sp</item>
    </style>
    <style name="MyTextTheme.Large" parent="MyTextTheme">
        <item name="android:textSize">17sp</item>
    </style>

    <!-- for default dialog theme -->
    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@drawable/default_dialog_bg_inset</item>
    </style>

</resources>
